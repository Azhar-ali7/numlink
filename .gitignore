# Claude Code local config / plans / memory
.claude/

# OS
.DS_Store

# Flutter build artifacts (the app has its own .gitignore too)
numlink_app/build/
numlink_app/.dart_tool/

# Serena tool config
.serena/

# Android Gradle build output
numlink_app/android/build/

# Local run logs
run.log

# Design handoff reference (local source of truth, not app code)
design-handoff-current/

# codegraph daemon runtime state — churns on every session
.codegraph/daemon.pid
